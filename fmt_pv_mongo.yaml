- apiVersion: v1
  kind: PersistentVolumeClaim
  metadata:
    labels:
      app: logging-fluentd
    name: logging-fluentd-mongodb-data
  spec:
    accessModes:
    - ReadWriteOnce
    resources:
      requests:
        storage: 100Gi
